import 'package:flutter/material.dart';

class SplashSlidertPage extends StatelessWidget {
  const SplashSlidertPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: PageView(children: [
      ],),
    );
  }
}
